/** Shared with About + Footer — matches in-view reveals */
export const EASE_OUT_SOFT = [0.16, 1, 0.3, 1];

/**
 * Replay fade/slide when the block leaves and re-enters the viewport
 * (scroll up or down).
 */
export const viewportScrollReplay = {
  once: false,
  amount: 0.15,
  margin: "0px 0px -12% 0px",
};
